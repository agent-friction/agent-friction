[package]
name = "agent-friction-core"
description = "Storage and analysis for agent-friction: permission events, tool failures, and the pattern collapsing behind its suggestions."
edition.workspace = true
version.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
chrono = { workspace = true }
regex = "1.13.1"
rusqlite = { workspace = true, features = ["bundled", "chrono", "serde_json"] }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
xdg = { workspace =  true }
